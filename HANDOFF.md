# HANDOFF — снимок состояния

**Дата:** 2026-06-17 (пред. чекпоинты 2026-06-14, 2026-06-08, 2026-06-03). Контекст и
бизнес-правила — в `CLAUDE.md`, запуск/деплой — в `README.md`. Здесь: что сделано, что
задеплоено, что открыто.

## Статус: в проде, работает ✅

- **Суточный отчёт** — cron **ежедневно 09:00 МСК**. Последний прогон (2026-06-08):
  288 позиций, 10 сообщений, 10/10.
- **Интерактивный бот меню** (с 2026-06-14) — постоянная служба `splithub-menu-bot.service`
  (long-polling): Поставщик→Бренд→Серия→% + блок «ключевые особенности».
- Репозиторий — единственный источник кода; деплой = `git pull` на сервере (+ рестарт
  службы бота для изменений бота).

## Что делает (кратко)

Раз в сутки читает БД сайта SplitHome (read-only), собирает кондиционеры в наличии
в Крыму (склад «Симферополь») с оптовыми ценами и шлёт владельцу в личный Telegram,
сгруппировав по поставщику и бренду. Подробности — `CLAUDE.md`.

## Сделано в этой итерации (2026-06-17): JAC — 4-й поставщик 🟨

Слито с веткой характеристик серии (origin/main): фича JAC наложена ПОВЕРХ блока
«ключевые особенности» — обе работают рядом. Коллизия имён файлов разрешена: JAC-ТТХ
вынесены в **`jac_specs.py`** (origin-`specs.py` — блок особенностей серии из БД — не
тронут), тесты JAC — в **`tests/test_jac_specs.py`**.

У поставщика **JAC** (b2b-jac.com) нет API — данные готовит отдельный скрапер
**`osatakti_mdv_b2b`** (рядом в GitHub) и кладёт в JSON; бот их читает (НЕ из БД).
Если файлов нет — JAC просто пропускается, отчёт по 3 поставщикам не падает.

- **`jac.py`** — читает `JAC_STOCK_JSON` (`jac_stock_latest.json`), применяет те же
  правила, что `db.py`: только Крым (колонка «Крым», «Больше N»→N), только
  кондиционеры (Холод кВт>0), без мультисплита/аксессуаров; опт = «Ваша цена»
  (→ `price_wholesale`). Бренд/серия — из пути категории (готовит скрапер).
- **`jac_specs.py`** — читает `JAC_SPECS_JSON` (`jac_specs_latest.json`, 77 ТТХ на
  модель), даёт компактную строку «❄ кВт · площадь · хладагент · тип».
- **Отчёт** (`main.py`): `rows += load_jac_rows()`; бренд жирным, у JAC серия
  рядом с моделью (`EUROKLIMAT Alba · EKSA-20HN`) — у остальных серия уже в title.
- **Меню наценки**: JAC — 4-й источник (код `j`) в `menu.py`; `bot.py` снапшот
  `+= load_jac_rows()`. По кнопке «✅ С характеристиками» под каждой моделью JAC —
  компактные ТТХ (`spec_lines`); у Бриз/Daichi/Русклимата та же кнопка даёт блок
  «ключевые особенности» из БД+УТП (`specs.build_specs_block`).
- `send_jac_test.py` — разовая отправка теста (остатки + меню+ТТХ) себе в Telegram.

**Настройка (.env бота):** `JAC_STOCK_JSON` + `JAC_SPECS_JSON` — пути к файлам
скрапера. Деплой: скрапер на том же VPS, cron `scrape`→`specs` ДО 09:00 МСК.

**Открыто по JAC:** согласовать УТП-набор характеристик (какие поля + где
выводить — компактно в меню / отдельной карточкой). Все 77 полей уже в
`jac_specs_latest.json`.

## Сделано в этой итерации (2026-06-03)

1. **Опт-цена Бриза из Бриз API** (`breez.py`): в БД сайта у Бриза розница, поэтому опт
   (base) берём напрямую из `/leftoversnew/` по `nc_code`, с откатом на цену БД.
   Фикс сайта в `sync_stock` для цены Бриза больше НЕ нужен. (`070af3e`)
2. **Надёжная доставка в Telegram**: keep-alive `Session` + ретраи, пауза между
   сообщениями, таймаут 30с (связь VPS→Telegram нестабильна, см. ниже).
   (`d0bb2f1`, `0994bcd`, `f03d80b`)
3. **Состав отчёта по фидбэку владельца** (`54464b6`, `27a4dd3`):
   - только Крым (Симферополь) для ВСЕХ поставщиков — Бриз больше не тянет
     материк/«под заказ»;
   - только кондиционеры нужных типов (категории Бытовые сплит / Полупромышленные /
     Мобильные) + фильтр `btu_calc > 0` — аксессуары/инструменты (мойки, развальцовки,
     насосы, шланги, сифоны, оконные адаптеры) отсеяны; мультисплит исключён;
   - формат: эмодзи на поставщика (🟦🟥🟩), **жирный бренд** впереди наименования,
     сортировка/группировка по бренду, полоски на всю ширину (тонкая после каждой
     позиции, двойная между брендами).

## Сделано в этой итерации (2026-06-08)

1. **Опт-цена Русклимата из `stock_stock.price_base`** (`7819b77`): в БД у Русклимата
   `price_wholesale` = розница (= `ric`), а настоящий опт («Ваша цена» портала
   b2b.rusklimat.com) лежит в `stock_stock.price_base` (per-warehouse). Бот читал
   розницу — теперь берёт `price_base`, откат на `price_wholesale`. Проверено на проде:
   НС-1598403 SHUFT Berg SFTO-07 → было 19 888 ₽ (розн), стало 13 722.72 ₽ (опт). 53/53
   строки Русклимата покрыты `price_base`. Бриз/Daichi не тронуты. (Промежуточная попытка
   `c32a3da` брала опт из фида Бриза — неверно: фид Бриза НЕ содержит NC Русклимата.)

2. **Инцидент: отчёт не приходил 06–08.06 — причина НЕ в боте.** 05.06 сервер
   перезагрузился (смена ядра), а стек сайта SplitHome (`oasis-*`, включая БД
   `oasis-db-1`) не имел авто-рестарта и не поднялся. Бот падал на коннекте к БД
   (`DB_HOST=invalid IP`, т.к. `run_daily.sh` не находил контейнер). После ручного
   подъёма сайта (`cd /opt/oasis && docker compose up -d`) — работает. **Урок:** бот
   молча не шлёт, если БД недоступна → стоит добавить уведомление владельцу о сбое
   сбора (открытый пункт ниже).

## Сделано в этой итерации (2026-06-14) — интерактивный бот меню

Отдельный long-polling бот (`stock_report_bot/bot.py`, служба `splithub-menu-bot.service`)
рядом с суточным отчётом: меню Поставщик→Бренд→Серия→% (наценка/скидка) → итог серии с
ценой-с-наценкой и фото внутреннего блока, готов к пересылке клиенту (шапка — только эмодзи,
поставщик/наценка не раскрываются).

1. **Блок «ключевые особенности» серии** (`specs.py`, `e371b6a`; UX `7f8cdce`): после
   выбора % — кнопки «✅ С характеристиками / 📄 Без». С характеристиками блок-цитата
   (`<blockquote>`) вклеивается в ТО ЖЕ сообщение (фото + модели + ТХ одним сообщением).
   Состав — 15 ТХ, согласован владельцем (класс энергоэфф., инвертор, компрессор, обогрев
   до −15 °C, шум, гарантия, Wi-Fi, голос-ассистенты, ионизация/плазма/УФ, самоочистка,
   SLEEP, авторестарт + «УТП»). ТХ из БД сайта (`catalog_producttech/techspec` →
   `db.fetch_tech_values`); названия у Бриз/Daichi/Русклимат РАЗНЫЕ → матч по ключевым
   словам + нормализация «сырых» значений (да/нет, Y/N, диапазоны, шум `a/b/c`); запасной
   детектор инвертора по названию позиции. **УТП Бриза — из Бриз API `/products/`**
   (`breez.fetch_breez_utp_by_nc`): синк сайта поле `utp` не сохраняет (в модели товара
   его нет), формат — двойной HTML-эскейп. `description` Бриза в БД уже есть (~97%) — не тянем.
   Спека: `docs/superpowers/specs/2026-06-13-tech-specs-button-design.md`.
2. **Кнопка «Меню»** (`af1bac4`): постоянная reply-кнопка «📋 Меню» (`is_persistent`,
   ставится на `/start`) + `setMyCommands` для нативной «☰ Menu» — владельцу не надо
   каждый раз писать `/start`.
3. **Короткие имена серий** (`a000b2a`): Бриз отдаёт «Инверторные сплит-системы серии
   CYCLONE DC Inverter» — в кнопке имя обрезалось. `menu.short_series` срезает префикс
   «…серии », показывает только серию (CYCLONE DC Inverter / PROGRESS). Идентичность
   (группировка/индексы в callback) — полная, изменение только отображения.
4. **Связь бота↔Telegram**: `run_bot.py` форсит IPv6 (как отчёт); connect-timeout
   `getUpdates` снижен до 10 с — отклик ровнее при сетевых блипах VPS↔Telegram.

Чистые функции покрыты тестами (без БД/сети); проверено на реальных данных всех 3
поставщиков и на живом Бриз API.

## Деплой (детали — в приватных ops-заметках, не в репозитории)

- На сервере: host-venv в `/opt/splithub_api_telegram` (`.venv`); обёртки `run_daily.sh`
  (резолвит IP контейнера БД сайта `oasis-db-1` и пишет в `.env` `DB_HOST`, затем запуск)
  и `run_report.py` (форс прямого IPv6 к Telegram). Бот — аналогично: `run_bot.sh` +
  `run_bot.py` (тоже форс IPv6), служба **`splithub-menu-bot.service`** (enabled).
  Обёртки серверные, не в git — переживают `git pull`, теряются при ре-клоне. ⚠️ Если
  `oasis-db-1` не запущен (напр. после ребута без авто-рестарта), `run_daily.sh` не находит
  IP и отчёт молча падает — см. инцидент 06–08.06 выше.
- **Деплой изменений бота:** `git push` → на сервере `git pull --ff-only` →
  `.venv/bin/python -m unittest discover -s tests` → `systemctl restart splithub-menu-bot.service`.
- **БД сайта** доступна только через docker-сеть сайта (не через хостовый порт 5432 —
  там БД другого проекта).
- **Telegram**: на VPS IPv4 к Telegram блокируется, socat-прокси ненадёжен для пачки
  крупных сообщений → используем **прямой IPv6** с хоста. НЕ добавлять `api.telegram.org`
  в глобальный `/etc/hosts` — сломает соседний host-бот `hotel_lead_bot`.
- Расписание: `crontab 0 9 * * *` (сервер в TZ Europe/Moscow → 09:00 МСК напрямую),
  лог `/var/log/splithub_telegram.log`.
- Доступ к серверу, токен бота, креды БД — НЕ в репозитории (репозиторий публичный).

## Формат согласован владельцем (2026-06-03)

Эмодзи поставщиков (🟦🟥🟩), жирный бренд, сортировка по бренду, полоски на всю
ширину (тонкая после каждой позиции, двойная между брендами) — приняты («всё ок»).

Остаётся по мере эксплуатации сверять, не появился ли в каталоге сайта новый мусор
(аксессуары с ошибочным `btu_calc`) и не пропало ли нужное.

## Открытые пункты

- **Уведомление о сбое сбора отчёта** (не реализовано): сейчас при недоступной БД бот
  падает молча и владелец узнаёт о простое лишь через несколько дней (см. инцидент
  06–08.06). Добавить отправку короткого алерта владельцу в Telegram при исключении в
  `main`. Согласовать с владельцем.
- **Daichi**: опт берётся из `price_wholesale` — отдельно свериться с порталом Daichi,
  верна ли цена (по аналогии с проверкой Русклимата).

## Тесты

`python -m unittest discover -s tests -v` — **50 тестов** (отчёт `report`, меню/серии/
наценка `menu`, блок характеристик `specs`, парсинг Бриз API `breez`), без БД и сети.
На сервере — тем же набором через `.venv/bin/python`.
